server:
  port: 8083

spring:
  application:
    name: account
  profiles:
    active: ${SPRING_PROFILES_ACTIVE}

  config:
    import: "optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}"

