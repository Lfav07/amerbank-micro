apiVersion: v1
kind: Service
metadata:
  name: audit-logging
  namespace: amerbank
spec:
  selector:
    app: auth-server
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
